document.addEventListener('DOMContentLoaded', () => {
    console.log('Página principal cargada.');

    // Aquí podrías agregar cualquier lógica que necesites para la página principal.

    // Por ejemplo, podrías manejar eventos de navegación o cualquier otra lógica global.
    // Como la página principal solo tiene enlaces a las otras secciones, no es necesario mucho código aquí.
});